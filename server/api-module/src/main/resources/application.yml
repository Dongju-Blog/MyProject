logging:
  level:
    root: DEBUG
spring:
  datasource:
    username:
    password:
    url:

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    generate-ddl: true
#    hibernate:
#      ddl-auto: create-drop
    show-sql: true
#  data:
#    rest:
#      base-path: /api
  security:
    securityKey: ${random.value}

server:
  port: 8081