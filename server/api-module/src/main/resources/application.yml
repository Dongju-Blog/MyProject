logging:
  level:
    root: DEBUG
spring:
  datasource:
    url: jdbc:log4jdbc:mariadb://${DB_IP}:${DB_PORT}/${DB_NAME}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

  jpa:
    database-platform: org.hibernate.dialect.MariaDBDialect
    hibernate:
      ddl-auto: update
    generate-ddl: true
#    hibernate:
#      ddl-auto: create-drop
    show-sql: true
#  data:
#    rest:
#      base-path: /api
  security:
    securityKey: ${random.value}

server:
  port: 8081