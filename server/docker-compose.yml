version: '3'
services:
  api-module:
    build: ./api-module
    image: back/api-module
    ports:
      - "8081:8081"
    environment:
      - "SPRING_PROFILES_ACTIVE=prod"
      - TZ=Asia/Seoul
    logging:
      driver: syslog
      options:
        tag: back